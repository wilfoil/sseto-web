import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#2972FE',
    },
    secondary: {
      main: '#131629',
    },
    text: {
      primary: '#FFF'
    }
  },
  
})

export default theme
