import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#347afc',
    },
    secondary: {
      main: '#131629',
    },
    text: {
      primary: '#FFF',
    }
  },
  
})

export default theme
