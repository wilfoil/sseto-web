import { StyledTextField } from "styles/common"

const LocationInput = () => {

    return <StyledTextField
    // error={!!error}
    // id={key}
    // name={key}
    label='Search location'
    variant="standard"
    // helperText={error}
    // value={value}
    // onChange={formik.handleChange}
  />
}

export default LocationInput;