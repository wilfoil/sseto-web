export enum ListingType {
    house = 'house',
    villa = 'villa',
    apartment = 'apartment',
    land = 'land',
    flat = 'flat'
}

export enum BucketFolders {
    listings = 'listings',
    profiles  = 'profiles'
}