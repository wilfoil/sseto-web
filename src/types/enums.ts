export enum ListingType {
    house = 'house',
    apartment = 'apartment',
    land = 'land',
    // flat = 'flat'
}

export enum BucketFolders {
    listings = 'listings',
    profiles  = 'profiles'
}